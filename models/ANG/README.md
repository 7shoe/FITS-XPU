# ANG model lane

`model.py` is the preserved ANG implementation placeholder. Export its `Model`
class from `__init__.py` once implemented. Add an Aurora `run_selected.sh` with
the same command-line contract as `scripts/FITS/aurora/run_selected.sh` when
ANG is trainable.
