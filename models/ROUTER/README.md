# ROUTER model lane

Place the ROUTER implementation in `model.py` and export its `Model` class from
`__init__.py`. Add an Aurora `run_selected.sh` with the same command-line
contract as `scripts/FITS/aurora/run_selected.sh` when ROUTER is trainable.
